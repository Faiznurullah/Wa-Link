CREATE DATABASE walink;

CREATE TABLE teman (
id int(25) NOT NULL AUTO_INCREMENT,
nama varchar(120) NOT NULL,
alamat varchar(200) NOT NULL,
nomor varchar(350) NOT NULL,

PRIMARY KEY(id)

);
